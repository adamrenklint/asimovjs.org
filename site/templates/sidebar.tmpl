<div class="sidebar">
  <ul class="sidebar-container">
    {{#children "/docs"}}

      <li {{#active this.url}}class="active"{{/active}}>
        {{link this.url this.title}}
      </li>

    {{/children}}
  </ul>
</div>