title: The server and you
---
text:

Once you start the server application using <code>node main.js</code>, there's a bunch of things that happens.

one server app, multiple client apps
multiple, re-usable client modules

Order of loading

## Logging

Logging happens on three levels: verbose, normal and error. Set <code>options.logVerbose</code> to <code>true</code>

Logging and verbose logging
Disable intro animation
Watching folders and recompiling
benchmarking built into logging

paths and configuration